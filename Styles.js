import { StyleSheet } from "react-native";

export const LoadingPageStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#ef8c52",
    alignItems: "center",
    justifyContent: "center",
  },
});
